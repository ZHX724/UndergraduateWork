<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.demo.blockchaintracksystem.mapper.BatchMapper">

    <resultMap id="BatchMap" type="org.demo.blockchaintracksystem.entity.Batch">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="batch_code" property="batchCode"/>
        <result column="production_date" property="productionDate"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="org.demo.blockchaintracksystem.entity.Batch"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO batch (product_id, batch_code, production_date, created_at)
        VALUES (#{productId}, #{batchCode}, #{productionDate}, #{createdAt})
    </insert>

    <select id="selectByProductId" resultMap="BatchMap">
        SELECT id, product_id, batch_code, production_date, created_at
        FROM batch
        WHERE product_id = #{productId}
        ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="BatchMap">
        SELECT id, product_id, batch_code, production_date, created_at
        FROM batch
        WHERE id = #{id}
        LIMIT 1
    </select>

    <delete id="deleteById">
        DELETE FROM batch WHERE id = #{id}
    </delete>
    <select id="selectByBatchCode" resultMap="BatchMap">
        SELECT id, product_id, batch_code, production_date, created_at
        FROM batch
        WHERE batch_code = #{batchCode}
        LIMIT 1
    </select>


</mapper>
