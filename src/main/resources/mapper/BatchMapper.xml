<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.demo.blockchaintracksystem.mapper.BatchMapper">

    <resultMap id="BatchMap" type="org.demo.blockchaintracksystem.entity.Batch">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="batch_code" property="batchCode"/>
        <result column="production_date" property="productionDate"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="org.demo.blockchaintracksystem.entity.Batch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO batch (product_id, batch_code, production_date, created_at)
        VALUES (#{productId}, #{batchCode}, #{productionDate}, NOW())
    </insert>

    <select id="selectByProductId" resultMap="BatchMap">
        SELECT id, product_id, batch_code, production_date, created_at
        FROM batch
        WHERE product_id = #{productId}
        ORDER BY id DESC
    </select>

    <!-- product 是否属于当前 uid -->
    <select id="countProductOwnedByUid" resultType="int">
        SELECT COUNT(1)
        FROM product
        WHERE id = #{productId} AND user_id = #{uid}
    </select>

    <!-- 删除：通过 join product 限制只能删自己的 -->
    <delete id="deleteByIdAndUid">
        DELETE b
        FROM batch b
        JOIN product p ON p.id = b.product_id
        WHERE b.id = #{batchId}
        AND p.user_id = #{uid}
    </delete>

</mapper>
